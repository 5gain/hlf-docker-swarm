caliper:
  fabric:
    gateway:
      localhost: false
      enabled: true
  logging:
    targets:
      console:
        options:
          # we don't care about info level messages anymore
          level: info 
